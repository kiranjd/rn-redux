import { hp } from '../utils';

const colors = {
  primary: '#d3d3d3',
  secondary: '#f3f3f0'
};

const commonHeaderStyle = {
  fontSize: 35,
  paddingHorizontal: 7,
  fontWeight: '300',
  marginTop: hp(1.5),
  fontVariant: ['tabular-nums']
};

const BORDER_RADIUS = 6;

export { colors, BORDER_RADIUS, commonHeaderStyle };
